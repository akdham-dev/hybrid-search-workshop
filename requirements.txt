# Core — needed for the whole workshop
numpy==2.1.3
rank-bm25==0.2.2            # reference BM25 (we also show a from-scratch version)

# Dense retrieval — the "real" embedding path
sentence-transformers==3.3.1   # downloads all-MiniLM-L6-v2 (~80MB) on first run

# Optional — only for the final LLM generation cell (Groq free tier)
groq

# Notebook
jupyter==1.1.1
